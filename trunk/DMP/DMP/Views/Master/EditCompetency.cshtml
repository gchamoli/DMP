@model DMP.ModelsView.CompetencyViewModel
@using DMP.Utils
<script src = "@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src = "@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
<div class="span4">
    @using (Ajax.BeginForm(new AjaxOptions { OnComplete = "closePopUp()", OnSuccess = "reLoad('/Master/Competency')" })) {
        @Html.HiddenFor(x => x.Competency.Id)
        <div>
            @Html.LabelFor(x => x.Competency.Name)
            @Html.TextBoxFor(x => x.Competency.Name)
        </div>
        <div>
            @Html.LabelFor(x => x.Competency.Designation)
            @Html.DropDownListFor(x => x.Competency.Designation, new SelectList(Model.Designations, "Value", "Value"))
        </div>
        <div>
            @Html.LabelFor(x => x.Competency.Description)
            @Html.TextAreaFor(x => x.Competency.Description)
        </div>
        <div>
            <input type="submit" value="Save" class="btn btn-primary" />
            <input type="reset" value="Reset" class="btn btn-primary" />
        </div>
    }
</div>
