@model IEnumerable<DMP.Models.StateModel>
@using DMP.Utils
@{
    ViewBag.Title = "States";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="/assets/plugins/fancybox/jquery.fancybox-1.3.4.css" rel="stylesheet"
    type="text/css" />
<script src="@Url.Content("~/Assets/plugins/fancybox/jquery.mousewheel-3.0.4.pack.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Assets/plugins/fancybox/jquery.fancybox-1.3.4.pack.js")" type="text/javascript"></script>
<script type="text/javascript">
    $(document).ready(function () {
        function refresh() {
            window.location.reload();
        }

        $('.editState').fancybox({
            'padding': '25px',
            'width': '300px',
            'height': 'auto',
            'autoScale': true,
            'autoDimensions': false,
            'scrolling': 'no',
            'easingIn': 'swing',
            'transitionIn': 'elastic',
            'transitionOut': 'elastic',
            'type': 'ajax',
            'centerOnScroll': true,
            'overlayShow': true,
            'enableEscapeButton': false,
            'hideOnOverlayClick': false,
            'titleShow': false,
            'onClosed': function () {
                //                refresh();
            }
        });
    });
</script>
<div class="row-fluid">
    <div class="span6 offset3">
        <table id="stateList" class="table table-bordered">
            <thead>
                <tr>
                    <th colspan="3" class="head1">
                        States
                    </th>
                    @if (User.IsInRole("HQ")) {
                        <th class="head1" style="text-align: center">
                            <a href="/Master/EditState/0" class="add editState" title="click here to add new state">
                            </a>
                        </th>
                    }
                </tr>
                <tr>
                    <th class="head2">
                        Name
                    </th>
                    <th class="head2">
                        Region
                    </th>
                    <th class="head2">
                        Description
                    </th>
                    @if (User.IsInRole("HQ")) {
                        <th class="head2">
                        </th>
                    }
                </tr>
            </thead>
            <tbody>
                @foreach (var state in Model) {
                    <tr>
                        <td>@state.Name
                        </td>
                        <td>@state.Region
                        </td>
                        <td>@state.Description
                        </td>
                        @if (User.IsInRole("HQ")) {
                            <td style="text-align: center">
                                <a href="/Master/EditState/@state.Id" class="edit editState" title="click here to edit state">
                                </a>
                            </td> 
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
